name: mobilecrypto
description: "MobileCrypto — crypto via mobile money en Afrique"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon/app_icon_beta.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon_beta.png"
  min_sdk_android: 21
